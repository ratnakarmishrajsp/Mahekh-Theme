<input type="hidden" value="true" id="srCheckoutAutoOnboardClient"/>
<link rel="stylesheet" href="https://fastrr-boost-ui.pickrr.com/assets/styles/sr-checkout.css">
<script src="https://fastrr-boost-ui.pickrr.com/assets/js/channels/shopify.js" defer></script>
<script src="https://fastrr-boost-ui.pickrr.com/assets/js/channels/shiprocketMarketing.js" defer></script>

 <!-- BTN - CUST - SCRIPT - START  -->
 <input type="hidden" value="true" id="shiprocketCheckoutBtnStylesCheck"/>
   <style>
    @keyframes srShimmer {
      0% { background-position: 100% 0; }
      30%, 100% { background-position: -100% 0; }
    }
    
    @keyframes srPulseHighlight {
      0%, 100% {
        transform: scale(1);
        box-shadow: 0 4px 15px rgba(204, 164, 59, 0.25) !important;
      }
      50% {
        transform: scale(1.03);
        box-shadow: 0 10px 28px rgba(204, 164, 59, 0.6) !important;
      }
    }

    .shiprocket-headless, .sr-headless-checkout {
      width: 100% !important;
      height: 48px !important;
      display: flex !important;
      align-items: center !important;
      justify-content: center !important;
      background: linear-gradient(120deg, #cca43b 25%, #ffd700 40%, #ffffff 50%, #ffd700 60%, #cca43b 75%) !important;
      background-size: 250% 100% !important;
      color: #1a1107 !important;
      font-weight: 700 !important;
      font-size: 16px !important;
      letter-spacing: 0.05em !important;
      border: none !important;
      border-radius: 6px !important;
      text-transform: uppercase !important;
      transition: all 0.3s ease !important;
      animation: srShimmer 4s infinite linear, srPulseHighlight 2.5s infinite ease-in-out !important;
    }
    
    .shiprocket-headless:hover, .sr-headless-checkout:hover {
      transform: scale(1.04) !important;
      filter: brightness(1.1) !important;
      box-shadow: 0 6px 20px rgba(204, 164, 59, 0.45) !important;
    }
   </style>
  <script>
    let btnStyles = null;
    {% if shop.metafields.shiprocketCheckoutButton.shiprocketCheckoutButtonStyles %}
      btnStyles = {{ shop.metafields.shiprocketCheckoutButton.shiprocketCheckoutButtonStyles }}
    {%  endif %}
        
    if (btnStyles && typeof btnStyles === 'object') {
      const styleElement = document.createElement("style");
      styleElement.type = "text/css";
      let cssRules = "";
  
      Object.entries(btnStyles).forEach(([className, styles]) => {
        // Convert styles object to CSS rules with !important
        const stylesString = Object.entries(styles)
          .filter(([key]) => key !== "text" && key !== "animation" && key !== "background-color" && key !== "background" && key !== "color") // Exclude overrides to preserve our gradient & color
          .map(([key, value]) => `${key}: ${value} !important;`)
          .join(" ");
  
         // Define the animation keyframes for highlight pulse
         let animationRule = `
           @keyframes srShimmer {
             0% { background-position: 100% 0; }
             30%, 100% { background-position: -100% 0; }
           }
           @keyframes srPulseHighlight {
             0%, 100% { transform: scale(1); box-shadow: 0 4px 15px rgba(204, 164, 59, 0.25) !important; }
             50% { transform: scale(1.03); box-shadow: 0 10px 28px rgba(204, 164, 59, 0.6) !important; }
           }
         `;
   
         // Base CSS rule for the class
         let cssRule = `
           ${animationRule}
   
           .${className} {
             ${stylesString}
             background: linear-gradient(120deg, #cca43b 25%, #ffd700 40%, #ffffff 50%, #ffd700 60%, #cca43b 75%) !important;
             background-size: 250% 100% !important;
             color: #1a1107 !important;
             font-weight: 700 !important;
             font-size: 16px !important;
             letter-spacing: 0.05em !important;
             font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif !important;
             animation: srShimmer 4s infinite linear, srPulseHighlight 2.5s infinite ease-in-out !important;
             transition: all 0.3s ease !important;
             height: 48px !important;
             display: flex !important;
             align-items: center !important;
             justify-content: center !important;
             border: none !important;
             border-radius: 6px !important;
             text-transform: uppercase !important;
           }
           .${className}:hover {
             transform: scale(1.04) !important;
             filter: brightness(1.1) !important;
             box-shadow: 0 6px 20px rgba(204, 164, 59, 0.45) !important;
           }
         `;
  
        // If 'text' key exists, append additional CSS rules
        if (styles.text) {
          cssRule += `
            .${className}:after {
              content: "${styles.text}";
              color: #1a1107 !important;
            }
            .${className} span {
              display: none !important;
            }
          `;
        }
  
        cssRules += cssRule;
      });
  
      // Set the CSS rules in the style element
      styleElement.textContent = cssRules;
      // Append the style element to the head
      document.head.appendChild(styleElement);
    }
  </script>
  <!-- BTN - CUST - SCRIPT - END  -->
